/**
 * Server Supabase client — заготовка для Server Components / Actions.
 */
export function createClient() {
  throw new Error(
    "Supabase server client ещё не подключён. Этап 1: Auth и профили.",
  );
}
